export const VALIDATION_KEYS_CONSTANTS = {
  REQUIRED: 'required',
  EMAIL: 'email',
  MIN_LENGTH: 'minlength',
  MAX_LENGTH: 'maxlength',
  PATTERN: 'pattern',
} as const;

export const VALIDATION_CONSTANTS = {
  MIN_PASSWORD_LENGTH: 6,
} as const;

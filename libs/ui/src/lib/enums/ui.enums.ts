export enum ESpacing {
  NONE = 'none',
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl',
}

export enum EAlign {
  START = 'start',
  CENTER = 'center',
  END = 'end',
  BETWEEN = 'between',
  STRETCH = 'stretch',
}

export enum EPageLayout {
  CENTERED = 'centered',
  DEFAULT = 'default',
}

export enum ESurfaceVariant {
  CARD = 'card',
  PLAIN = 'plain',
}
